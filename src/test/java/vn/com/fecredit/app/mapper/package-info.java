/**
 * Tests for mapper implementations.
 *
 * This package contains tests that verify the correct mapping behavior
 * between entities, DTOs, and other object types using Spring-managed mapper components.
 *
 * Test classes:
 * - UserMapperTest: Tests mapping between User entity and DTOs
 *
 * Testing approach:
 * - Verifies field mapping correctness
 * - Tests null handling
 * - Tests collections mapping
 * - Tests bidirectional mapping
 * - Uses custom test data
 * - Validates with custom assertions
 *
 * @see org.springframework.stereotype.Component
 */
package vn.com.fecredit.app.mapper;